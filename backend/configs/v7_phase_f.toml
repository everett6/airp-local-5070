description = "Frozen 2026-09-16 before its first run (Phase F, docs/RESEARCH_OPTIMIZATION.md). Same 100 stocks, window and grid as v5_fund_top100, plus: one-word UP/DOWN answers scored from token log-probabilities (llm_lp, llm_fund_lp; primary arm llm_fund_lp), exact Newton stacker fits, the classical-anomaly composite and logistic (anomaly_rank, anomaly_logit), the Kronos-small candlestick model (pre-training ends June 2024; forecasts made first by scripts/kronos_forecasts.py), and the RL agent seeing all of them (rl_state v7). Verbalized arms replay v5's cached answers. Served by the tuned Ollama on :11435 (flash attention, 4 parallel slots; recorded in provenance). Success criteria pre-registered in docs/EXECUTION_PLAN.md (Phase F)."
tag = "v7_phase_f"
model = "qwen3:8b"
target = "abs"
start = "2025-06-02"
end = "2026-09-02"
horizon = 5
step = 5
warmup = 12
reflect_every = 4
data = "data/prices_pit_2025-06-02_top100.csv"
fund = true
rl = true
score_logprob = true
solver = "newton"
anomalies = true
ohlcv = "data/ohlcv_pit_2025-06-02_top100.csv"
kronos = true
rl_state = "v7"
ollama_url = "http://127.0.0.1:11435"
